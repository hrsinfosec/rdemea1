name: Build custom RustDesk

on:
  workflow_dispatch:

jobs:
  call-custom-windows-build:
    uses: ./.github/workflows/custom-windows-build.yml
    with:
      upload-artifact: true
      upload-tag: custom
    secrets: inherit
