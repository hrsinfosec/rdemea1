# .github/workflows/build-custom.yml
name: Build custom RustDesk

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  call-flutter-build:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
